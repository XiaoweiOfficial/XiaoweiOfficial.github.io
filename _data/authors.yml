# Additional authors.
#
# Only needed if a page or post is written by someone other than the site
# author defined in _config.yml. Set `author: "Their Name"` in that page's
# front matter and add a matching entry here.
#
# Xiaowei Zhang is the site author and is configured in _config.yml, so he
# does not need an entry here.
